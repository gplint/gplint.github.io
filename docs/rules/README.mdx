import DocCardList from '@theme/DocCardList';

# Rules

List of rules. Some rules have icons next with the names, that icons have the following meaning:

| Icon                                           | Description                                     |
|------------------------------------------------|-------------------------------------------------|
| <div style={{'text-align': 'center'}}>⚙️</div> | Rule have configuration properties.             |
| <div style={{'text-align': 'center'}}>🪄</div> | Rule allows autofix with `--fix` cli parameter. |

<DocCardList />
